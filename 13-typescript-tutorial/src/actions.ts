export function sayHello(name: string): void {
  console.log(`Hello ${name}`);
}

export let person = 'samad';

type Student = {
  name: string;
  age: number;
};

export const newStudent = {
  name: 'john',
  age: 20,
};
